# 31. モック操作確認 / 検証プラン

今回の改修（発注/棚卸の属性分離・自社加工品の登録経路・費目の手動選択・既存商品の取り込み）について、
**仕様モック (`index.html`) 上で確認できる範囲の「変更箇所」と「操作確認手順」** を定義する。

> 役割分担：
> - **本書 (31)** … モックで確認可能な層（UI 挙動・画面操作）。
> - **[30. 検証計画](./30_verification.md)** … モックでは確認不可な層（(a) MUGEN 実データ突合 / (b) 71万件の負荷）。

## 確認環境
- ローカル：`index.html` をブラウザで直接開く（ビルド不要、CDN 依存なし）。
- 公開：GitHub Pages — https://suguru789987.github.io/inventory-product-management/
- 状態はインメモリ（リロードで初期化）。1テストごとにリロード推奨。

---

## A. 変更箇所一覧（`index.html`）

| # | 変更内容 | 該当箇所 |
|---|---|---|
| 1 | 一括バー文言「棚卸対象に含める/から外す」＋補足「既存の仕入商品を棚卸対象に取り込めます」 | `index.html:828, 831` |
| 2 | データ画面に「発注対象」「費目」行を追加 | `index.html:915, 919` / JS `1359-1360` |
| 3 | 編集画面に「発注対象」(参照) ＋「費目」(編集可セレクト) を追加 | `index.html:1024, 1046` / JS `1378, 1381, 1398` |
| 4 | 新規追加画面に「費目」セレクタ(必須・手動) ＋補足文言 | `index.html:1140` / JS `1413, 1427, 1432, 1444` |
| 5 | データモデルに `orderOn`(発注対象)・`category`(費目) を全商品へ付与 | `index.html:1200-1230` |
| 6 | 費目マスタ定数 `categoryOptions`(仮ラベル5種) | `index.html:1234` |
| 7 | 費目セレクタの動的生成 `populateCategorySelects()` | `index.html:1465-` |
| 8 | 一括操作トースト文言を「棚卸対象に含めました/から外しました」へ | JS `bulkSetStatus` |

docs 側の変更：[10](./10_data_model.md)(orderOn/category/二属性)、[20](./20_business_rules.md)(移行方針・費目ルール)、[02/03/04](./02_screen_data.md)(各画面)、[90](./90_open_questions.md)(OQ-11/12)。

---

## B. 操作確認チェックリスト

各ケース「手順 → 期待結果」。結果欄の ✅ は **jsdom でモックのJSを実機実行して自動確認済み（2026-05-20、15/15 PASS）**。`☐(要目視)` はブラウザ目視で確認する項目。

| ID | 観点 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| TC-01 | 漏れ解消(一覧表示) | 一覧画面を開く | 発注先を持たない書き起こし商品も含め全24件が表示される（旧UIで出なかった `delivery` 商品が一覧に並ぶ） | ✅ |
| TC-02 | データ画面・新項目 | 「生ビール（樽）20L」のデータを開く | 「発注対象＝発注対象外」「費目＝酒類仕入高」が表示される | ✅ |
| TC-03 | 発注対象=true の表示 | 「紙ナプキン（100枚入）」のデータを開く | 「発注対象＝発注対象」「費目＝消耗品費」と表示（唯一の発注対象） | ✅ |
| TC-04 | 編集・発注対象は参照のみ | 任意商品の「編集」を開く | 「発注対象」欄が `disabled`（グレー・編集不可）で表示される | ✅ |
| TC-05 | 編集・費目変更 | 編集で費目を別の値に変更し「更新する」 | トースト「更新しました」→ 同商品のデータ画面で費目が変更後の値になっている | ✅ |
| TC-06 | 追加・費目必須バリデーション | 新規追加で商品名のみ入力し「登録する」 | `費目を選択してください` のアラート（登録されない） | ✅ |
| TC-07 | 追加・正常登録 | 新規追加で 商品名/費目/単位/税区分 を入力し「登録する」 | トースト「商品を登録しました」→ 一覧に追加。データ画面で「発注対象＝発注対象外」「費目＝選択値」「棚卸＝表示」 | ✅ |
| TC-08 | 追加・補足文言 | 新規追加画面を開く | 「発注対象にはなりません」「AI推定は行いません」の補足が表示される | ✅ |
| TC-09 | 一括・棚卸取り込み | 棚卸OFFの商品にチェック → 「一括：棚卸対象に含める」 | 棚卸が「表示」化、トースト「棚卸対象に含めました」。バーに「既存の仕入商品を棚卸対象に取り込めます」表示 | ✅ |
| TC-10 | 一括・除外 | 棚卸ONの商品にチェック → 「一括：棚卸対象から外す」 | 棚卸が「非表示」化、トースト「棚卸対象から外しました」 | ✅ |
| TC-11 | 属性独立の確認 | 「紙ナプキン」(発注対象)を棚卸ONに切替 → データ画面確認 | 棚卸＝表示 / 発注対象＝発注対象（**発注属性は変わらない**＝二属性が独立） | ✅ |

### 回帰確認（既存機能が壊れていないか）
| ID | 観点 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| TR-01 | 商品名検索 | 検索ボックスに「ビール」 | 該当のみ表示 | ✅ |
| TR-02 | 登録経路フィルタ | 「インフォマート連携」で絞込 | 該当のみ表示（routeは行内に出ないため件数＋目視で確認） | ☐(要目視) |
| TR-03 | 棚卸ステータス絞込 | ラジオ「非表示」 | 非表示商品のみ表示 | ✅ |
| TR-04 | 削除確認ダイアログ | 編集で「削除する」 | 確認ダイアログ表示（モックでは削除自体は未実装） | ☐(要目視) |

> 自動確認は jsdom 上での DOM/ロジック検証（`/tmp/mocktest/run.js` 相当）。色・レイアウト・実ブラウザのダイアログ表示は `☐(要目視)` をブラウザで最終確認する。

---

## C. 合否基準
- TC-01〜11 がすべて期待通り（属性分離・費目手動選択・取り込み動線が成立）。
- TR-01〜04 に回帰がない。
- ❌ が出た場合は A. 変更箇所一覧の該当行を起点に修正。

> 本書はモックの**概念実証レベル**の確認。「数値が合うか」「71万件で耐えるか」は [30. 検証計画](./30_verification.md) でステージング検証する。

## 関連
- [30. 検証計画](./30_verification.md) — 実データ突合 / 負荷検証
- [01〜04 画面仕様](./01_screen_list.md) — 各画面の詳細
